# Local dev only (vite dev). Without this the "start again" link on the code
# screen resolves against the signup origin, which has no login page.
# Must match the port apps/admin/vite.config.ts pins.
VITE_ADMIN_BASE=http://localhost:5173
